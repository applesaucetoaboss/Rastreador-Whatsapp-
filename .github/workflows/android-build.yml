name: Build Android APK (unzip + generate wrapper if placeholder + build)

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: write

jobs:
  android:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          persist-credentials: true
          fetch-depth: 0

      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Install common packages (wget/unzip/git/rsync)
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y wget unzip git ca-certificates apt-transport-https gnupg rsync
        shell: bash



      - name: Set GRADLE_VERSION environment variable
        run: echo "GRADLE_VERSION=8.7" >> $GITHUB_ENV
        shell: bash

      - name: Install Gradle CLI 8.7 (for wrapper generation)
        run: |
          set -euo pipefail
          GRADLE_VERSION=${{ env.GRADLE_VERSION }}
          GRADLE_DIR="$RUNNER_TEMP/gradle"
          mkdir -p "$GRADLE_DIR"
          wget -q "https://services.gradle.org/distributions/gradle-${GRADLE_VERSION}-bin.zip" -O gradle.zip
          unzip -q gradle.zip -d "$GRADLE_DIR"
          echo "$GRADLE_DIR/gradle-${GRADLE_VERSION}/bin" >> $GITHUB_PATH
          rm -f gradle.zip
        shell: bash

      - name: Install Android command-line tools and SDK packages
        run: |
          ANDROID_SDK_ROOT="$RUNNER_TEMP/android-sdk"
          mkdir -p "$ANDROID_SDK_ROOT/cmdline-tools"
          wget -q https://dl.google.com/android/repository/commandlinetools-linux-9477386_latest.zip -O cmdline.zip
          unzip -q cmdline.zip -d "$ANDROID_SDK_ROOT/cmdline-tools"
          mkdir -p "$ANDROID_SDK_ROOT/cmdline-tools/latest"
          mv "$ANDROID_SDK_ROOT/cmdline-tools/cmdline-tools/"* "$ANDROID_SDK_ROOT/cmdline-tools/latest/" || true
          # expose sdkmanager and platform-tools to following steps
          echo "$ANDROID_SDK_ROOT/cmdline-tools/latest/bin" >> $GITHUB_PATH
          echo "$ANDROID_SDK_ROOT/platform-tools" >> $GITHUB_PATH
          yes | "$ANDROID_SDK_ROOT/cmdline-tools/latest/bin/sdkmanager" --sdk_root="$ANDROID_SDK_ROOT" "platform-tools" "platforms;android-34" "build-tools;34.0.0" || true
          yes | "$ANDROID_SDK_ROOT/cmdline-tools/latest/bin/sdkmanager" --sdk_root="$ANDROID_SDK_ROOT" --licenses || true
          echo "ANDROID_SDK_ROOT=$ANDROID_SDK_ROOT" >> $GITHUB_ENV
          echo "ANDROID_HOME=$ANDROID_SDK_ROOT" >> $GITHUB_ENV
        shell: bash

      - name: If there's a zip, list and unzip to extracted/ then merge into repo root
        run: |
          set -euo pipefail
          mkdir -p extracted
          shopt -s nullglob || true
          zips=( *.zip )
          if [ "${#zips[@]}" -gt 0 ]; then
            echo "ZIP files found: ${zips[*]}"
            for z in "${zips[@]}"; do
              echo "Unzipping $z -> extracted/"
              unzip -o "$z" -d extracted
            done
            echo "Merging extracted contents into repo root (preserving .git)"
            rsync -a --exclude '.git' extracted/ ./
            ls -la
          else
            echo "No zip files found in repo root."
          fi
        shell: bash

      - name: Ensure Gradle wrapper exists (generate if placeholder or missing)
        run: |
          set -euo pipefail
          # Use the downloaded Gradle CLI (in PATH) to generate a real wrapper if needed
          if [ ! -f ./gradlew ] || grep -q "placeholder gradlew" ./gradlew 2>/dev/null || grep -q "placeholder gradlew" ./gradlew.bat 2>/dev/null; then
            echo "Gradle wrapper missing or placeholder detected — generating wrapper with downloaded gradle"
            gradle wrapper --gradle-version 8.7
          else
            echo "Gradle wrapper appears present and not placeholder"
          fi
          # Always ensure gradlew is executable on Linux runners
          chmod +x ./gradlew || true
        shell: bash

      



      - name: Cache Gradle packages
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/gradle-wrapper.properties') }}
          restore-keys: |
            ${{ runner.os }}-gradle-

      

      - name: Inject build secrets
        env:
          STRIPE_PUBLISHABLE_KEY: ${{ secrets.STRIPE_PUBLISHABLE_KEY }}
          BACKEND_BASE_URL: ${{ secrets.BACKEND_BASE_URL }}
        run: |
          echo "Injecting build-time config via gradle.properties override"
          mkdir -p ~/.gradle
          {
            echo "STRIPE_PUBLISHABLE_KEY=${STRIPE_PUBLISHABLE_KEY}" 
            echo "BACKEND_BASE_URL=${BACKEND_BASE_URL}" 
          } >> ~/.gradle/gradle.properties
        shell: bash

      - name: Run unit tests
        run: |
          ./gradlew test --no-daemon --stacktrace --console=plain --info
        shell: bash

      - name: Build APK (assembleDebug)
        env:
          ANDROID_SDK_ROOT: ${{ env.ANDROID_SDK_ROOT }}
          ANDROID_HOME: ${{ env.ANDROID_HOME }}
        run: |
          set -euo pipefail
          export TERM=dumb
          # Capture full gradle output to a file, then on failure print a concise summary
          ./gradlew assembleDebug --no-daemon --stacktrace --console=plain --info | tee build.log
          EXIT_CODE=${PIPESTATUS[0]}
          if [ "$EXIT_CODE" -ne 0 ]; then
            echo "\n--- Build failed: concise error summary (last 300 lines) ---"
            tail -n 300 build.log
            echo "--- End summary ---\n"
          fi
          exit "$EXIT_CODE"
        shell: bash

      - name: Upload Debug APK artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: app-debug-apk
          path: app/build/outputs/**/*.apk

      - name: Upload Gradle build log
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: gradle-build-log
          path: build.log

      - name: Commit minimal logs to ci-logs branch
        if: always()
        run: |
          set -euo pipefail
          mkdir -p gh_actions_logs/${{ github.run_id }}
          if [ -f build.log ]; then cp -f build.log gh_actions_logs/${{ github.run_id }}/build.log; fi
          ./gradlew -v > gh_actions_logs/${{ github.run_id }}/gradle-env.txt || true
          echo "RUN_ID=${{ github.run_id }}" > gh_actions_logs/${{ github.run_id }}/env.txt
          echo "JAVA_VERSION=$(java -version 2>&1 | head -n1)" >> gh_actions_logs/${{ github.run_id }}/env.txt
          git config user.name "github-actions"
          git config user.email "github-actions@users.noreply.github.com"
          git fetch origin ci-logs || true
          # Base local ci-logs on remote if it exists; otherwise create new
          if git show-ref --verify --quiet refs/remotes/origin/ci-logs; then
            git checkout -B ci-logs origin/ci-logs
          else
            git checkout -B ci-logs
          fi
          git add gh_actions_logs/${{ github.run_id }}/
          git commit -m "CI build log for run ${{ github.run_id }} [skip ci]" || true
          # Push with lease to avoid non-fast-forward rejection
          git push --force-with-lease origin ci-logs
        shell: bash

  precheck:
    runs-on: ubuntu-latest
    outputs:
      has_keystore: ${{ steps.detect.outputs.has_keystore }}
    steps:
      - name: Detect keystore secret presence
        id: detect
        env:
          ANDROID_KEYSTORE_BASE64: ${{ secrets.ANDROID_KEYSTORE_BASE64 }}
        run: |
          if [ -n "$ANDROID_KEYSTORE_BASE64" ]; then
            echo "has_keystore=true" >> $GITHUB_OUTPUT
          else
            echo "has_keystore=false" >> $GITHUB_OUTPUT
          fi
        shell: bash

  release:
    needs: [android, precheck]
    if: ${{ needs.precheck.outputs.has_keystore == 'true' }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          persist-credentials: true
          fetch-depth: 0

      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Prepare keystore
        env:
          ANDROID_KEYSTORE_BASE64: ${{ secrets.ANDROID_KEYSTORE_BASE64 }}
        run: |
          echo "$ANDROID_KEYSTORE_BASE64" | base64 -d > release.keystore
        shell: bash

      - name: Create keystore.properties
        env:
          ANDROID_KEYSTORE_PASSWORD: ${{ secrets.ANDROID_KEYSTORE_PASSWORD }}
          ANDROID_KEY_ALIAS: ${{ secrets.ANDROID_KEY_ALIAS }}
          ANDROID_KEY_PASSWORD: ${{ secrets.ANDROID_KEY_PASSWORD }}
        run: |
          cat > keystore.properties << EOF
          storeFile=../release.keystore
          storePassword=${ANDROID_KEYSTORE_PASSWORD}
          keyAlias=${ANDROID_KEY_ALIAS}
          keyPassword=${ANDROID_KEY_PASSWORD}
          EOF
        shell: bash

      - name: Build Release APK
        run: |
          ./gradlew assembleRelease --no-daemon --stacktrace --console=plain --info
        shell: bash

      - name: Upload Release APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: app-release-apk
          path: app/build/outputs/**/*.apk

  server-check:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Use Node 18
        uses: actions/setup-node@v4
        with:
          node-version: '18'
      - name: Install server deps and run basic check
        working-directory: server
        run: |
          set -euo pipefail
          if [ -f package-lock.json ]; then
            npm ci
          else
            npm install --no-audit --no-fund
          fi
          node -e "console.log('Server files present; Stripe version ok')"
